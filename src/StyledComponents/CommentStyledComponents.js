import styled from "styled-components";

export const diaglogBackdrop = styled.dig`
  background: "black";
`;
export const CommentContainer = styled.dialog``;

export const CommentHeader = styled.div``;

export const CommentBody = styled.div``;
